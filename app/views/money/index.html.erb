<p id="notice"><%= notice %></p>
<div class="wrapper">
<h1>家計簿</h1>
<p class="button"><%= link_to '金額を入力', new_money_path %></p>
<table>
  <thead>
    <tr>
      <th>項目名</th>
      <th>金額</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @money.each do |money| %>
      <tr>
        <td><%= money.name %></td>
        <td><%= money.item %></td>
        <td><%= link_to '確認', money %></td>
        <td><%= link_to '編集', edit_money_path(money) %></td>
        <td><%= link_to '削除', money, method: :delete, data: { confirm: '本当にいいですか？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<hr />
<br>
<%= Date.today %> 現在のみちるんのお財布
<p>合計金額: <%= @money.sum(:item) %></p>
</div>
